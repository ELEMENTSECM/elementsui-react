import { css } from "react-emotion";

export const styles = {
	spinner: css`
		position: relative;
		justify-content: center;
		flex: 0 0 auto;
		display: flex;
		align-items: center;
		padding: 10px;
	`,
	spinnerLabel: css`margin-left: 10px;`
};
