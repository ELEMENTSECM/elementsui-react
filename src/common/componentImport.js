import * as React from 'react';
import PropTypes from 'prop-types';
import { initializeIcons } from 'office-ui-fabric-react/lib/Icons';
initializeIcons();
module.exports = {React, PropTypes, initializeIcons}